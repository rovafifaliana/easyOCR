# easyOCR
Document reader

# Execution
[bash] ./run.sh --type company --input path/ot/input/folder --output path/to/output/folder --dpi 200 --skip-existing

# Type
The `--type` parameter accepts the following values :
ot : for `Ordre de virement` document
company : for `Statut de société` document
